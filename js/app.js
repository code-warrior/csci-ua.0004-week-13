window.onload = () => {
    let x = (10 * 10) + 20;
    let y = 1;
    let sum = (x + y);
    let firstName = `Ed`;
    let lastName = `Bob`;
    const UNIVERSITY = `NYU`;

    lastName = `Norton`;

    console.log(`Hey, x + y is ${sum}`);
    console.log(typeof sum);
    console.log(`${firstName} ${lastName}`);
    console.log(typeof name);
    console.log(`${firstName} ${lastName} does not attend ${UNIVERSITY}`)
};
